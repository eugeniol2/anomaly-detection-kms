# Ambiente do TCC - deteccao de anomalias em metadados de auditoria de KMS
#
# Python 3.11.9. Criar e ativar:
#   py -3.11 -m venv .venv
#   .venv\Scripts\Activate.ps1
#   pip install -r requirements.txt
#
# Versoes fixadas: a reprodutibilidade das 330 execucoes depende delas.
# Para recriar o ambiente byte a byte, usar requirements-lock.txt.

# M1 populacao, M2 trafego, M3 ataque
# Fluxos de aleatoriedade separados por subsistema (D-003) via
# numpy.random.SeedSequence.spawn, derivados da mesma semente.
numpy==2.4.6

# M1 a M8 - leitura e escrita de CSV com cabecalho, manipulacao tabular
pandas==3.0.5

# M10 avaliacao - Wilcoxon pareado (D-027)
scipy==1.17.1

# M7 particao  - GroupShuffleSplit / StratifiedGroupKFold, particao por sessao (D-018)
# M9 modelos   - arvores e ensembles (D-026)
# M10 avaliacao- matriz de confusao, F1, precisao, revocacao, especificidade (D-021, D-022)
#                arvore de profundidade 1 da verificacao de trivialidade (D-028)
scikit-learn==1.9.1

# M10 avaliacao - correcao de Holm sobre as 22 comparacoes (D-027),
# via statsmodels.stats.multitest.multipletests(method="holm")
statsmodels==0.15.0

# Figuras da monografia
matplotlib==3.11.1

# Verificacao de determinismo dos modulos (mesma semente -> mesma saida)
pytest==9.1.1
